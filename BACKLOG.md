# AIRLUXO — Backlog

Deferred ideas, captured so they're not lost.

## DevOps & deployment — 🚧 IN PROGRESS (production live 1 Jun 2026)
Repo + Vercel + airluxo.ch are DONE; remaining = staging environment. Setup agreed 31 May 2026.

> **Project path moved** from `~/Desktop/airluxo` → **`~/Documents/airluxo`** (1 Jun 2026) to escape a macOS TCC write-block on Desktop files. Launch Claude Code from the new path. (Memory keyed to the old Desktop path still applies.)

- ✅ **Git + GitHub — DONE.** Repo **github.com/francoluumos/airluxo** (account `francoluumos`). `.gitignore` excludes `.env`/`node_modules`/`dist`/`.claude`/`.agents`. Branches **`main`** (prod) + **`staging`** pushed. Push auth via dedicated `~/.ssh/id_ed25519_francoluumos` key + repo-local `core.sshCommand` (`-F /dev/null`) — see memory `airluxo-git`.
- ✅ **Vercel — DONE.** Vite SPA, project `airluxo`. The 5 `VITE_*` env vars set for **Production + Preview**. Auto-deploys on push to `main`.
- ✅ **Domain airluxo.ch — LIVE with SSL.** Hostpoint DNS (nameservers NOT moved → email preserved): apex `A @ → 216.198.79.1`, `www CNAME → cname.vercel-dns.com`; deleted old Hostpoint apex `A 217.26.48.101` + `AAAA 2a00:d70:0:a::166` (were splitting traffic). MX intact. Verified `https://airluxo.ch` serves 200.
  - _Optional polish:_ www CNAME → Vercel's newer `50e82785b89bf626.vercel-dns-017.com` (clears "DNS Change Recommended"); set www to **redirect** to apex for a clean canonical (currently serves 200).
- **Branching:** feature branch (`feat/...`) off `main` → PR (auto preview URL) → merge to `staging` (soak-test) → `staging → main` (ship). Full GitFlow judged overkill for a solo founder.
- **REMAINING — staging environment:**
  - ✅ **`staging.airluxo.ch` Vercel domain — DONE.** Mapped to the `staging` branch (Hostpoint `CNAME staging → cname.vercel-dns.com`); auto-deploys on push and serves live (verified 1–2 Jun 2026).
  - ⏸️ **Separate Supabase project for staging — DEFERRED 2 Jun 2026 (no real data on prod yet).** Today staging + prod share one project (`shoeopxxjawmusgnjxfh`); fine while prod is empty — test with throwaway emails (TESTING.md already assumes this). **Prerequisite before splitting (do this first, valuable on its own): capture the prod schema as code** — `supabase db dump --schema-only` → `supabase/migrations/` (none exist today; schema lives only in the live DB, so any hand-clone would drift). Then choose the isolation mechanism: a **separate staging project** (apply the migrations + re-create secrets/auth/storage/Stripe once) or **Supabase branching** (git-integrated preview DBs; needs migrations-as-code; paid add-on). When done, give the Vercel **Preview** scope its own `VITE_SUPABASE_*` values. **Trigger to revisit: before onboarding real partners or starting marketing** (i.e. before real data lands on prod).
  - ⬜ Add **`staging.airluxo.ch`** to the PostHog internal-traffic `$host` filter (project 190604) so staging never pollutes real numbers (localhost already excluded).
- ⬜ **Before real launch:** swap Stripe `pk_test_` → live key (Production scope only) when ready to take real money.

## Authentication & accounts
- **Customer (driver) accounts & profile — ✅ BUILT 1 Jun 2026 (pending Supabase Auth config).** Airbnb-style guest accounts: combined log-in/sign-up modal (`AuthModal.jsx`) from a top-right account menu (`AccountMenu.jsx` in `Nav`), **book without login → account offered on the confirmation** (`PostBookingAccount`; the booking auto-links by email; the embed stays anonymous), and a profile (`CustomerAccount.jsx`, route `account`) with **My Trips / Saved cars / Licence on file / Account settings**. v1 auth = **Google + Email magic link** (passwordless). DB: `customers` + `favourites` tables (RLS own), `bookings.user_id` + a customer SELECT policy (`user_id` OR JWT-email match → auto-links pre-account bookings). `handle_new_user` trigger now routes signups to `customers` vs `partners` by intent. `AuthProvider` loads both profiles; bookings stamp `user_id`; verified licences persist to `customers.licence` and prefill future bookings; saved-car hearts on `CarCard`. See plan `eager-waddling-torvalds.md`.
  - **⬜ Remaining config (no code):** (1) **Google OAuth client** (id/secret) in Supabase Auth → Providers — the Google button errors until this is set. (2) **Resend custom SMTP** in Supabase Auth for reliable prod magic-link delivery (built-in mailer works for dev/testing, rate-limited). (3) URL allowlist: Site URL + redirects for `airluxo.ch`, `staging.airluxo.ch`, `localhost:5173`. (4) Confirm email signups enabled in Auth settings.
  - **Known v1 limitation:** Google/email both redirect away, so the "resume this exact booking after login" intent isn't restored — the user returns signed-in and re-clicks Reserve (now passes the gate). Persisting + replaying the intent across the redirect is a follow-up.
  - **✅ Book-then-account — DONE 1 Jun 2026 (replaced the login gate).** Guests now **complete a booking without logging in**; the confirmation screen offers one-tap account creation (Google or an email sign-in link, prefilled with the booking email) via `PostBookingAccount` in `CarDetail`. The booking auto-links to the new account by email (SELECT policy matches `guest_email`). Embed stays anonymous. (Reverses the earlier "login required to book" choice — confirmed during testing that the gate hurt conversion.)
  - **Phone OTP sign-in — DEFERRED to a later increment.** Add SMS one-time-code as a third method. Supabase Auth's *native* phone providers are **Twilio, MessageBird/Bird, Vonage, Textlocal**. **OneSignal** (the user's suggestion) is primarily push + SMS *marketing*, not a native Supabase phone-auth provider — using it would mean a **custom OTP edge function** (generate code → send via OneSignal/other SMS API → verify) rather than Supabase's built-in `signInWithOtp({phone})`. Either way it carries a per-SMS cost + Swiss/EU deliverability checks. Decide native-provider (simplest) vs OneSignal/custom before building.
- **Password reset flow — DONE.** "Forgot password?" on the partner login sends a Supabase reset email (`resetPasswordForEmail`, redirect `?reset=1`); the recovery link lands on `ResetPassword.jsx` (recovery session via `detectSessionInUrl` → `updateUser({ password })`). A **Change password** card is in dashboard Settings (`updateUser`). _Config needed:_ Supabase Auth → URL Configuration must allowlist the app origin + `/?reset=1` redirect; reliable email delivery needs custom SMTP (Resend) — the built-in Supabase mailer is heavily rate-limited (see verified-sender-domain gap).
- **Social / OAuth login (later).** Let partners sign in with **Google** (and optionally Apple / Microsoft) via Supabase Auth OAuth providers, in addition to email + password — faster onboarding, no password to forget. Needs the provider configured in Supabase (client id/secret + redirect URLs) and "Sign in with Google" buttons on the partner login. Map the OAuth user to a `partners` row on first sign-in (company name still needed, so a light post-signup step to capture it).

## Analytics & privacy
- **Cookie consent + privacy policy — DONE (draft).** Opt-in cookie banner (`CookieBanner.jsx`, consent stored in localStorage); a draft **Privacy & Cookie Policy** at `?privacy` (`PrivacyPolicy.jsx`) with a prominent **lawyer-review disclaimer**. Footer "Privacy" + "Cookies" links wired. _Before launch:_ have counsel review/finalise the policy, confirm processor list + data-residency claims, and decide cookieless vs cookie-based analytics.
- **Product analytics (PostHog) — DONE (consent-gated, awaiting key).** `src/lib/analytics.js` lazy-loads `posthog-js` only after analytics consent AND a `VITE_POSTHOG_KEY` is set (EU host via `VITE_POSTHOG_HOST`); otherwise a silent no-op. Events tracked: `search_performed` (city + results_count → demand-vs-supply), `car_viewed`, `map_pin_clicked`, and the booking funnel (`booking_started` → `booking_licence_step` → `booking_payment_step` → `booking_confirmed`). _Next:_ paste the PostHog project key into `.env`; build the funnel + "searched cities with low results_count" insight in PostHog; add partner-dashboard usage events.
  - _v2:_ mirror `search_performed` + booking-stage events into a Supabase `events` table to join demand against live `listings`/`partner_locations` for a precise partner-acquisition report.

## Email & notifications
- ✅ **Transactional email foundation — DONE (Resend; verified 4 Jun 2026).** Provider = **Resend**; `send.airluxo.ch` sending domain verified (SPF/DKIM/DMARC); all `RESEND_*` secrets set; **Supabase Auth custom SMTP → Resend** configured (magic-link / password-reset deliver through Resend). Confirmed live in the Resend logs. See [EMAIL.md](EMAIL.md).
- ✅ **Branded template system — DONE 4 Jun 2026.** `supabase/functions/_shared/email.ts` is the single source of truth (`emailShell` + `rows`/`button`/`chf`/`esc`); brand once, applies everywhere. Marketing blasts use Resend → Broadcasts.
- ✅ **Guest invoice / receipt — DONE 4 Jun 2026.** `booking-invoice` emails the guest a branded receipt (full price breakdown, reference, deposit-waived note) fired server-side from `stripe-capture` the moment the card is charged (→ Confirmed). _Not a formal VAT invoice — see follow-ups._
- ✅ **All transactional emails on the shared shell — DONE 4 Jun 2026.** `booking-confirm`, `booking-notify` (was unbranded system-ui) and the `newsletter-subscribe` welcome all refactored to import `_shared/email.ts`. One source of truth for brand styling.
- 🚧 **Marketing lifecycle flows (lean in-house) — STARTED 4 Jun 2026.** Decision: own the orchestration on Supabase + Resend (no external ESP, no second list); campaigns via Resend Broadcasts; AI as a copy-drafting assistant with human review. Foundation built: `marketing_sends` log (idempotent), per-subscriber `unsubscribe_token` + `newsletter-unsubscribe` function (one-click List-Unsubscribe, verified), consent gate via the SSOT, `pg_cron` + `pg_net` + Vault for scheduling/auth. **First flow live: `marketing-birthday`** (daily, branded note + member gesture). _⚠️ Activation: store the service-role key in Vault as `sb_service_role_key` (see EMAIL.md) — until then the cron 403s._
  - ✅ **Flows control panel — DONE 4 Jun 2026.** Marketing → Flows: per-flow status/stats, pause/resume, recent-sends history (`admin_marketing_overview` / `admin_set_flow_active`).
  - ✅ **Four more flows — DONE 4 Jun 2026** via the generic `marketing-run` runner: post-trip (2d after completion), win-back (no booking 180d), wishlist (saved cars, no booking 60d), new-models (weekly digest, sends only if new arrivals). Each consent-gated + re-send throttled; cron-scheduled.
  - ✅ **Abandoned-booking recovery — DONE 4 Jun 2026** (legal guardrails confirmed by Franco; pattern used in other projects). `checkout-lead` captures email + car/dates at the details step → `checkout_leads`; one same-car reminder 1–48h later via the `abandoned` flow (soft opt-in basis), suppressed on conversion (trigger marks leads converted), explicit opt-out, or prior recovery; one-click unsubscribe handles lead tokens; hourly cron.
  - **⬜ Next:** **AI drafting** step (Claude → subject/preheader/body into `emailShell`, merge-tag templates, human review) in the Flows panel. Per-flow **preview + test-send** from the panel. **`locale` per customer** (DE/FR/IT/EN) for language routing. **`news.airluxo.ch`** subdomain split before volume ramps. Bounce/complaint suppression sync from Resend webhooks.
- **⬜ Remaining follow-ups:**
  - **Partner statement email** (periodic payout/commission summary — needs aggregation + a schedule/cron).
  - **Formal VAT invoice + PDF** (sequential numbering, VAT reg. no., per-supply VAT split partner-rental vs AIRLUXO-fee, PDF attachment) before B2B/expense use.
  - **Notification routing** (below) — alerts → support contact, invoices → billing contact.
  - **Newsletter abuse hardening** — captcha/rate-limit on the public `newsletter-subscribe` endpoint (Turnstile/hCaptcha or HMAC unsubscribe links).
- **Route notifications to the right profile contacts.** `booking-notify` currently emails the partner's **login (auth) email**. Now that the profile captures **support** / **billing** contacts and an **invoice-only email**, route by topic: booking/operational alerts → support (or a dedicated notifications) email; invoices & statements → `invoice_email` / `billing_email`. Add per-event recipient settings + let the partner choose which address receives what. Also build the actual invoice/statement emails (none exist yet).

## WhatsApp notifications (draft — planned 4 Jun 2026)
Transactional WhatsApp alongside email: **booking confirmation, pick-up reminder (day before), rental-docs delivery**, later two-way support.
- **Tool:** WhatsApp Cloud API (Meta) via a BSP — **360dialog** recommended (EU/CH data residency, GDPR, flat per-conversation pricing, direct Cloud API). Twilio = pricier alternative with the best DX.
- **Architecture (mirrors email):** a `whatsapp-send` edge function called from the same triggers (booking created/confirmed → confirmation; a daily cron → pick-up reminder before `start_date`; on-demand → docs as a document message). Pre-approved **Utility** templates (transactional, cheap, allowed); **Marketing** templates separate + explicit opt-in. WhatsApp opt-in at checkout (phone already collected); send log like `marketing_sends`. Inbound webhook for replies later.
- **Prereqs (account/ops, before code):** Meta Business verification + WhatsApp Business Account, a BSP account, template approval (~1–2 days). So: set up the account first, then wire the function.

## Localization / i18n (planned 5 Jun 2026)
Multi-language for the **customer frontend, customer account, and partner dashboard** (founder/admin stays English — internal). Languages in order: **EN (source) → DE → FR → IT** (Swiss set). Auto-detected from the browser, always overridable, persisted per user.

**Four layers (each different work):** (1) UI strings, (2) user/AI content (car descriptions etc.), (3) emails (transactional + marketing), (4) formatting (dates/numbers/currency via `Intl`). _Honest headline: infrastructure is moderate; **extracting every hardcoded string into keys is the big mechanical job** — AI speeds translating, not extraction. Phase it._

**Stack & architecture (decided in brainstorm):**
- **`react-i18next`** — JSON catalogs, namespaces (`customer` / `partner`), runtime switch, lazy-load, browser-language detection.
- **Hybrid source of truth:** **English in the repo** (canonical keys); **DE/FR/IT in Supabase** (`translations` table: `namespace, key, locale, value, source_hash, updated_at, auto`), loaded at runtime (cached in localStorage), falling back to EN. This makes the admin Translation section a natural DB editor + AI-fill — no CI needed to edit translations.
- **Detect + set:** map `navigator.language` (`de-CH`→`de`, etc.), switcher in nav/footer, persist to **localStorage + a `locale` field on customers/partners** (same field the marketing-email language routing uses). Logged-out = localStorage + detect.
- **SEO (later):** locale in the URL (`/de/…` or `?lang=`) so Google indexes each language for the public marketplace; v1 cookie/localStorage-based.

**Admin Translation section (founder dashboard):**
- Coverage table: namespace · key · EN (source) · DE/FR/IT, each tagged **translated / missing / stale**.
- **Stale detection** via `source_hash` — if the EN source changes, its translations flip to "review".
- **AI translate** per-cell / per-column ("fill all missing DE") / bulk, via a `translate` edge function (Claude) with brand-voice guidance (luxury tone, keep `{placeholders}` intact, never translate "AIRLUXO"/brand names). Coverage % per locale.

**Phases:**
1. **Setup** — react-i18next provider + detection + language switcher; `locale` on customer/partner profiles; `translations` table + admin Translation section shell + `translate` edge fn. Ship EN + DE scaffolding.
2. **Customer marketplace + booking flow** — extract strings (highest-value, customer-facing) → EN + DE.
3. **Customer account**, then **partner dashboard** — extract + translate.
4. **Emails** (locale-aware `_shared/email.ts` + flow copy, keyed off the recipient's `locale`) + **per-locale AI car descriptions** (content layer).
5. **Add FR, then IT** — mostly "AI fills the new column, human reviews"; consider URL locales for SEO at this point.

**Open decisions:** (a) car descriptions per-locale vs single-language for v1 — recommend single-language first, per-locale in Phase 4; (b) confirmed: all UI translations in Supabase (not repo JSON) for the AI-admin-editor workflow.

## Integrations
- **Embeddable booking widget — v1 DONE.** `?embed=<partnerId>` renders a chrome-less partner fleet + full booking flow (`Embed.jsx`); dashboard Overview has an iframe-snippet generator (EmbedCard). _v2 refinements:_ dedicated embed key (revocable) instead of partner id; postMessage auto-height; theming (light/dark + accent); allowed-origins/CSP; gate by plan (Pro/Max); single-car embed variant.
- **Webhook out (partner's own system) — v1 DONE.** Settings → Webhook: partner sets an **endpoint URL** + Enabled toggle; AIRLUXO POSTs JSON on `booking.created` / `booking.updated` (fired from `createBooking` + dashboard status changes) via the `booking-webhook` edge function. Each delivery is HMAC-SHA256 signed (`X-AIRLUXO-Signature: sha256=…`) with a per-partner `webhook_secret` shown in the UI (reveal/copy/regenerate). A **"Send test event"** button posts a sample payload and reports the HTTP status. _v2:_ delivery log + retries/backoff; per-event subscription toggles; multiple endpoints; Zapier/Make recipe templates (the raw webhook already works with a Zapier "Catch Hook" trigger).
- **Settings menu — DONE.** New dashboard **Settings** section consolidating integrations: Webhook (above), **Calendar subscription** (ICS feed via `calendar-feed`, per-partner `calendar_token`, subscribe URL with copy), and the **Embed** module (moved out of Overview).

## Booking
- **Group ride / "ride with a friend" — multi-car booking.** Let a group rent **several cars together** in one flow — e.g. a road trip or event with 3 supercars. Needs: multi-car selection (a cart/"add another car"), a **combined availability check** across all chosen cars + the same date window, and **one checkout** for the group. Data model: a parent **group booking** linking child bookings — likely **one child booking per car**, which may span **multiple partners** → multiple Stripe destination charges/payouts under a single guest payment (Stripe supports multiple transfers, or charge-then-transfer). Each car needs its **own verified driver** (per-car licence assignment — the renter may not drive all of them), so the licence-KYC step repeats per car/driver. Optional **group discount** (ties into the promo/referral system). Per-partner notifications, and **all-or-nothing vs partial** confirmation + cancellation/refund handling across the group. Marketing hook: "book the whole convoy." Sizeable feature — scope a v1 (e.g. single-partner groups first, then cross-partner).
- **Driver's licence verification step (KYC) with AI autofill.** Add a verification step to the booking flow: the guest scans their driver's licence with their phone (camera capture / upload). An AI/OCR pass reads the licence and **auto-fills**: first name, last name, birth date, first day of validity (issue date), permit categories/types, and permit (document) number. Guest reviews/edits, then the booking proceeds. Considerations: front + back capture, mobile camera UX, store images securely (private bucket, not public), data-protection/retention (Swiss/EU — licence data is sensitive PII), partner sees verification status (verified/pending/failed), block confirm until verified. Likely an Edge Function calling a vision model (e.g. Gemini) for extraction, similar to the studio-shot pattern.

## Partner profile / logistics
- **Pick-up location & opening hours — DONE.** Dashboard **Location** section manages **multiple pick-up sites** (`partner_locations`), each with a structured **address autofilled from Swiss geo.admin.ch** (street/number/zip/city/country + lat/lng), **phone + email**, and its **own opening hours** (`opening_hours` jsonb per weekday + `allow_after_hours` + `after_hours_fee`, all per-location now — the old `partners.*` hours columns are unused). Locations are editable in place. Cars are assigned a `location_id` on the "list a car" pricing step (pick an existing site or create one inline). The guest booking flow reads a narrow public RPC (`listing_logistics`, SECURITY DEFINER — keeps the partners table owner-only) returning the **location's** hours/address, to **show the pick-up location** and **constrain the time picker** (after-hours allowed → amber note + surcharge shown; not allowed → blocks out-of-hours times). The homepage map (`FleetMap.jsx`) pins cars by city.
  - ✅ **After-hours surcharge is now charged** — added to the authoritative server recompute in `stripe-create-payment` (computes "outside hours" from the location's `opening_hours` + the chosen pickup/return times, adds `after_hours_fee` once), shown as an "After-hours handover" line in the guest price breakdown.
  - ✅ **Map pins use real coordinates** — `fleet_pins()` RPC (SECURITY DEFINER, anon-safe) returns each car's location lat/lng; the homepage map pins at the precise site and falls back to a city centroid only when a location has no coordinates yet.
  - _Remaining follow-up:_ pick-up address on the booking confirmation + ICS event.
  - **Address autocomplete uses the Swiss Federal geo API (`api3.geo.admin.ch`) — Switzerland-only.** Free, no key. **To onboard international partners we must switch to Google Places (or another global geocoder):** create a Google Cloud key with billing + Places API, swap the provider in `src/lib/geocode.js` (keep the same parsed shape: street/number/zip/city/country/lat/lng), and relax the CH-only assumption (the form already has a Country selector). geo.admin.ch returns nothing for non-CH addresses, so int'l partners currently can't autofill.

## Fleet (priority: after Stripe Increment 2, then after the driver's-licence check)
- **Manual car blocking / internal hold.** From the dashboard (per car in My fleet), let the partner block a car for specific date ranges so it can't be booked — e.g. maintenance, owner use, servicing. Each block captures **who blocked it (name)** and a **reason**. Blocks are **unbookable** dates (marketplace availability respects them) and appear in the **Calendar** and **Bookings** views flagged as *internally blocked* (visually distinct from guest bookings). Likely a `car_blocks` table (listing_id, start_date, end_date, blocked_by_name, reason, created_by) with the calendar/booking views merging blocks + bookings.

## Monetization — partner subscription tiers
**🚧 Mechanics BUILT 1 Jun 2026 (manual plan; self-serve billing TODO).** `partners.plan` (free/pro/max); per-tier commission (15/9/3%) applied server-side in `stripe-create-payment` and mirrored in the dashboard (`src/lib/plans.js` is the single source of truth); car limits enforced on new listings via the `enforce_car_limit` trigger + UI guard (Free 3 / Pro 25 / Max ∞, existing cars untouched); Plans tab shows the live plan, commission, and car usage. **Plan is set manually** (update `partners.plan` in Supabase) until billing is wired.
- **⬜ Remaining:** **Stripe Billing/Subscriptions** for the monthly fee (Checkout to subscribe + customer portal to manage + webhook to sync `plan`/status + proration on up/downgrade); feature gates (priority/featured placement, analytics, API access, team members) by plan; downgrade UX when over the new plan's limit (today: new-listing block only).

Original draft notes:
- **Tiers (draft numbers — tune later):**
  - **Free** — CHF 0/mo · up to **3 cars** · **15%** commission · standard placement, AI studio thumbnails, calendar sync. Low barrier to acquire supply; high take-rate.
  - **Pro** — CHF 49/mo · up to **25 cars** · **9%** commission · priority placement, analytics, faster payouts. Pays for itself once monthly volume makes the lower take-rate beat the fee.
  - **Max** — CHF 199/mo · **unlimited cars** · **3%** commission · featured placement, team members, API access, dedicated support. For large operators where commission would dominate.
- **Why tiers:** marketplace+SaaS hybrid — partners self-select up as they grow (trade commission for a subscription).
- **To build:** `partners.plan` column (free/pro/max); Stripe **Billing/Subscriptions** for the monthly fee; **enforce car limits** on listing creation; **apply the tier's commission rate** in `stripe-create-payment` (replace the flat 3% host commission) + the dashboard earnings math; gate features (placement, analytics, API) by plan; upgrade/downgrade flow + proration.

## Founder / admin dashboard (AIRLUXO back office)
A **company-internal** dashboard for the founder — distinct from the partner dashboard — to run the platform. Gated to founder accounts only (e.g. an `is_admin` flag on a profile / an allow-listed email + a dedicated route or subdomain like `admin.airluxo.ch`; never client-trusted — enforce with RLS / an admin-only edge function using the service role).
- **Platform KPIs:** GMV, bookings, take-rate / net revenue, active partners & cars, fleet utilisation, demand-vs-supply by city (we already log `search_performed` results_count in PostHog), funnel drop-off.
- **Promo / referral admin:** create & manage promo codes (the v1 stopgap is editing the Supabase table directly), see **redemptions per code/affiliate** and **commission owed** for out-of-band payouts. (Direct successor to the promo-code system.)
- **Partner management:** review/approve new partners, see Stripe-Connect payout status, suspend/flag, impersonate-for-support (read-only).
- **Customer overview:** accounts, bookings, licence-verification status (sensitive PII — strict access + audit).
- **Finance:** payouts, application-fee revenue, refunds/cancellations, affiliate commissions, export for accounting.
- **Marketing ops:** the AI content pipeline (MARKETING #4), campaign/UTM performance, channel attribution.
- **Build notes:** likely its own route/area reusing the dashboard shell; most reads need a service-role edge function or admin-scoped RLS since founders must see across all partners/customers (normal RLS scopes to own rows). Start read-only (KPIs + promo redemptions) and add management actions incrementally.

### 🚧 Implementation plan — founder dashboard + prospect pipeline (planned 3 Jun 2026; first build target)

**Anchor feature: the prospect/onboarding pipeline (CRM).** Sales tool — Franco creates a *preview* for a potential partner (uploads their cars, configures everything) with **no prospect email required**, shows them a storefront + dashboard preview, and on agreement **claims it into a live partner account** with zero data rebuild. Modelled as a CRM pipeline board in the founder dashboard.

**Access & routing (answer to "admin.airluxo.ch?"):** the **URL is not the security boundary** — gating must be server-side. So:
- **Security:** an `app_admins(user_id)` allowlist + a SECURITY DEFINER `is_admin()` helper, enforced in **every admin read/write** (admin-scoped edge functions using the service role + RLS). Never client-trusted. Seed Franco's user id.
- **Surface v1:** a gated in-app route (e.g. `?admin`) rendered only for `is_admin` — fastest, reuses the existing auth/session and dashboard shell.
- **`admin.airluxo.ch` later (optional polish):** map it as a Vercel domain alias to the same app and render the admin area on that hostname; lets us add a separate access gate. Cosmetic/operational, not the security layer — so route-first, subdomain later.

**Prospect model:** a prospect IS a real `partners` row owned by a **placeholder auth user** (internal email `prospect-<uuid>@prospect.airluxo.ch`, no real email needed), flagged `is_prospect=true`, hidden from the public marketplace, reachable via a secret `preview_token`. Keeps the `partners.id → auth.users.id` FK + RLS + the real partner dashboard working unchanged. **Go-live = claim** (swap the placeholder email for the partner's real one, flip the flag) — every car/photo/setting stays intact.

**Data model:** CRM fields on `partners` (unified; a prospect is a pre-partner):
- `is_prospect bool`, `preview_token uuid`, `pipeline_stage text` (Lead → Preview built → Shared → Negotiating → Won → Lost), `prospect_contact_name/email/phone` (all optional), `prospect_source`, `prospect_notes`.
- Denormalize an exclusion flag so prospect cars never leak into public reads: add the `is_prospect` filter to `fetchPublicListings` + `fetchFleetPins` **and** an RLS guard on the public listings read. Prospect cars stay status `Available` (so the embed shows them) — hidden only by the prospect flag, not by `Draft`.
- (Option to split CRM fields into a separate `prospects` table later if it grows; on `partners` is simplest for v1 + makes claim a trivial in-place update.)

**Admin edge functions (service-role, `is_admin`-checked):**
- `admin-create-prospect` — create placeholder user + partners row (is_prospect, preview_token, stage=Lead, company/contact). Returns partner_id + preview link.
- `admin-impersonate-prospect` — mint a magic link (admin `generateLink`) for the placeholder user so Franco builds the fleet in the **existing partner dashboard** (full reuse, no new editor).
- `admin-claim-prospect` — set the placeholder user's email to the partner's real one, send a set-password/magic link, flip `is_prospect=false` on partner + listings, stage=Won. Then prompt Stripe Connect onboarding.

**Previews to show the prospect:**
- **Storefront** → reuse the **embed** (`?embed=<prospectPartnerId>`, token-gated, "Preview" banner) — their fleet + the real booking flow.
- **Dashboard** → Franco demos live via the impersonation link; a read-only shareable dashboard tour is a v2 nicety.

**Phases (build order):**
1. ✅ **Phase 0 — admin foundation — DONE 3 Jun 2026.** `app_admins` + `is_admin()` + `FounderApp` gating (login → not-authorized → shell) rendered on `admin.` host or `?admin`; seeded Franco.
2. ✅ **Phase 1 — prospect create + pipeline board — DONE 3 Jun 2026.** partners CRM fields + `admin-create-prospect` (placeholder user via `handle_new_user`) + `admin_list_prospects`/`admin_set_prospect_stage`/`admin_update_prospect` RPCs + the pipeline board (stage columns, cards w/ company/contact/car-count/embed preview link, create modal, stage moves).
3. ✅ **Phase 2 — build-out — DONE 3 Jun 2026.** `admin-impersonate-prospect` (magic link → build the fleet via the real dashboard, lands on `?partner`) + prospects hidden from the public marketplace + map (denormalized `listings.is_prospect` + sync triggers; `fetchPublicListings` filter + `fleet_pins` exclusion).
4. ✅ **Phase 3 — previews — DONE 3 Jun 2026.** Tightened public listings RLS to exclude prospects; token-gated `preview_listings(partner_id, token)` RPC; embed shows prospect cars only with `?preview=<token>` + a "Sales preview" banner; prospect cards link `?embed=<id>&preview=<token>`.
5. ✅ **Phase 4 — claim → live — DONE 3 Jun 2026.** `admin-claim-prospect` (swaps placeholder email for the partner's real one, flips `is_prospect=false` → cars go live via trigger, stage=Won, returns a password-setup link) + "Go live" claim modal on the card. _Follow-up: Stripe Connect onboarding is still done by the partner in their dashboard after claim — could prompt it._

**Customers cockpit follow-ups (section DONE 3 Jun 2026 — searchable list + detail sheet + newsletter consent trail):**
- **Footer newsletter signups don't appear in the Customers list.** The footer (`NewsletterSignup` → `newsletter-subscribe` edge fn) writes **only to the Resend Audience**, never to `customers` — so an email-only subscriber has no Supabase row and isn't visible in the founder cockpit. `customers.marketing_opt_in` is set only for people who already have a customer row (signed-in toggle, checkout opt-in, or guest opt-in adopted on sign-up). Net: **Resend = full subscriber list; Supabase Customers = customers who opted in** — the two diverge. _If founder visibility/auditability of all subscribers is wanted: either persist footer signups to a lightweight `newsletter_subscribers` table (email + source + consent timestamp, no auth user) and surface them in the cockpit, or pull the Resend Audience into the Marketing section via the Resend API. Decide which is the source of truth first._
- **Newsletter actions from the cockpit (currently read-only):** resend welcome, manual unsubscribe, CSV export of opted-in customers, birthday/activation-mail segment (birthdate already captured from the licence).

## Growth & operations enablers
_Captured from [MARKETING.md](MARKETING.md) and [OPERATIONS.md](OPERATIONS.md) — the product pieces those ideas depend on._
- **Referral / promo-code / affiliate tracking.** Unique discount/referral codes with attribution + partner commissions. Unlocks the hotel-concierge and luxury-Airbnb-host channels (give each partner a trackable code, pay commission on attributed bookings). Needs: a `promo_codes` / `referrals` model, code redemption in the booking flow (discount applied + server-validated), attribution + a partner/affiliate payout view. Pair with UTM tagging (we already have PostHog) to measure channel performance.
- **Zero security deposit.** Remove the cash deposit (à la editionrent.ch).
  - ✅ **Stage A — damage-protection / zero-excess add-on — DONE 1 Jun 2026.** Per-listing add-on (`listings.protection_available` / `protection_fee` / `deposit_amount`): the guest pays a per-trip protection fee to drop their damage excess to CHF 0 and skip the security deposit. **Partner-keeps**: the fee sits *outside* the subtotal in `stripe-create-payment` (v13, live) so it carries **no** guest service fee and **no** host commission — the partner receives 100% (app fee stays `service + commission`). Toggle + breakdown row in `CarDetail`; config in both PartnerDashboard listing forms; recorded on `bookings.protection` / `protection_fee` / `deposit_amount`. Test = TESTING.md §8. **This is the primary "no deposit" mechanism.**
  - ⛔ **Stage B — refundable pre-auth hold — SHELVED 1 Jun 2026 (wrong primitive).** The plan was a second manual-capture PaymentIntent held until return. Killed because **Stripe card authorisations expire after ~7 days**: rentals are booked weeks ahead and run longer than a week, so the hold lapses before the trip. The unused DB columns `bookings.deposit_payment_intent_id` / `deposit_status` were provisioned for this and now sit idle (harmless; reusable below).
  - **↪ Better alternative if a safety net is ever needed for guests who decline protection: save-card-on-file + charge-on-damage.** Use a Stripe **SetupIntent** to store a reusable payment method (no funds held → no 7-day expiry), with explicit guest consent to be charged for damages up to `deposit_amount`. On a damage claim, charge the saved card off-session. Reuse the idle `deposit_*` columns for the SetupIntent / charge ids + status. This is the real follow-up, not a funds hold.
  - **Long term:** a **master marketplace insurance** (AXA / Die Mobiliar / Allianz) for true platform-wide zero-deposit + a per-booking protection fee. Watch: claims/excess flow, fraud (lean on licence KYC), VAT on the fee. See OPERATIONS.md / INSURANCE.md.
- **"With driver" / chauffeur add-on.** Per-listing option with hourly + daily driver rate (reuses the add-on pattern + price-breakdown line), a "With driver" filter, and a **licensing attestation** at partner onboarding (passenger transport is regulated in CH). See OPERATIONS.md.

## Loyalty & referral program (recurring customers) 💎
**Why:** Airbnb's referral program drove ~900% growth; loyalty/tiers (Booking.com Genius) compound retention. For a high-ticket luxury rental, repeat + referred guests are the cheapest, highest-LTV demand. Strategy + creative incentives live in [MARKETING.md #7](MARKETING.md); this is the build spec. **Brand rule:** frame as membership / credits / access / upgrades, NOT loud discounts (a luxury brand shouldn't read "coupon").

**What already exists (the partial base):** `promo_codes` + `validate_promo` RPC + `funded_by` (platform/partner) + affiliate `commission_*` + `bookings.promo_code/discount_amount/affiliate_commission`, and the authoritative discount path in `stripe-create-payment` + the `PromoField` in checkout. Referral can largely **reuse** this (a per-customer referral code). **New** = points + tiers + redemption.

**Data model (new):**
- `customers.loyalty_points` (int balance) + `loyalty_tier` (stored, recomputed on change).
- `loyalty_ledger` (append-only: `customer_id, delta, reason, booking_id?, created_at`) — source of truth; balance = sum. Never edit past rows.
- `referrals` (`referrer_id, referee_email/id, code, status, reward_amount, created_at`) — or extend `promo_codes` with an `owner_customer_id` for per-customer referral codes + double-sided attribution.
- `src/lib/loyalty.js` — single source of truth for tier thresholds + earn/burn rates (mirror the `plans.js` pattern; mirror the constant server-side like `GUEST_SERVICE`).

**Earn/burn rules (draft):**
- **Earn** points on **completed** trips only (award at capture / trip end, not at booking — avoids gaming via cancellations): X pts per CHF on the base+addons; bonus for referrals converting, reviews, group bookings, midweek/idle-fleet trips (ties to utilization).
- **Burn** at checkout: comp a **damage-protection** or **delivery** add-on, member credit off the next trip, category **upgrade**, or waived service fee — implemented as an authoritative adjustment in `stripe-create-payment` (same place discounts are clamped), never client-trusted.

**Tiers (draft, name on-brand — e.g. "Keys": Silver → Gold → Platinum → Noir):** thresholds by trips or trailing-12-month CHF; each tier unlocks perks (see MARKETING #7). Consider a **status-match** to acquire HNW travellers fast.

**Phased build:**
1. ✅ **Foundation — DONE 2 Jun 2026.** `loyalty.js` SSOT + `customers.loyalty_points` + `loyalty_ledger` + `award_loyalty_on_completion` trigger (earns rental value × 5 on a booking's first transition to `Completed`, idempotent).
2. ✅ **Surfaced — DONE.** "Membership" tab in `CustomerAccount`: Key tier, points + ~CHF value, progress to next tier, perks.
3. ✅ **Referral — DONE.** `customers.referral_code` (auto) + `referred_by`; `apply_referral(code)` RPC (claimed from `?ref=` once the customer row exists); double-sided bonus (referrer 1000 / referee 500) on the referee's first completed trip; "Invite friends" card in the Membership tab.
4. ✅ **Points redemption — DONE.** Authoritative in `stripe-create-payment` (v14): points → CHF member credit, AIRLUXO-funded, clamped to its own margin (never reduces partner payout); burned on booking insert via trigger; "Use my N points" control + member-credit line at checkout.
5. ✅ **Phase 4b — tier comps — DONE 3 Jun 2026 (AIRLUXO-funded).** Decision: AIRLUXO absorbs. Free **damage protection** + **delivery** (Platinum) and the **service-fee waiver** (Noir) are comped authoritatively in `stripe-create-payment` from the customer's tier (completed-trip count, mirrors `loyalty.js`), clamped to AIRLUXO's margin so the partner payout is never reduced; shown as a "{tier} benefit" line at the payment step. **Still manual/not-in-price-engine:** free **upgrade** (a car swap, concierge), **priority/early access** (UX perk), and **experiential** (Noir track day etc.). _Optional follow-up: a pre-payment benefits hint before the payment step._

## Listing media
- **Per-listing video — v1 DONE (manual upload).** Partners upload a short clip in Add/Edit car (`listings.video_url`, reuses the public `listing-photos` bucket; ≤25 MB client check). It plays **muted/loop on hover** on the car card (desktop only; lazy `preload="none"`, src set on mouseenter and unloaded on leave, so the grid downloads no video until hovered) and **muted autoplay loop in the booking detail**. Honours `prefers-reduced-motion`; mobile keeps the image in the grid (no hover) and only plays in the booking view. Works in the embed too (shared components).
  - _v1 follow-ups:_ strip the audio track + transcode/compress on upload (ffmpeg.wasm) to enforce ≤3 MB / 720p; use the studio thumbnail as the `<video poster>`; a small "has video" indicator on the card; per-listing video in the white-label embed config.
- **Per-listing video — v2: AI generation from a prompt (Google API).** Generate the studio clip server-side instead of manual upload: an Edge Function calling Google's video model (**Veo via the Gemini API**, e.g. `veo-3.x`/`gemini` video generation) with a fixed prompt — same locked composition as the still (car facing left, slow orbit/parallax, pure-white seamless background, no audio) seeded from the car's studio image. Considerations: video generation is **slow (async/long-running op — poll the operation) and costly**, so do it on-demand (a "Generate video" button) not automatically; cap length; store result to the bucket → `video_url`; strip audio; budget/quota guard. Mirrors the `studio-shot` pattern but for video.

- **Studio-shot thumbnail generator — on `gemini-3-pro-image` (Nano Banana Pro) as of 2 Jun 2026.** The `studio-shot` edge function (v11) re-renders an uploaded car photo as a clean 3/4 white-studio shot; output geometry locked via `imageConfig.aspectRatio: "16:9"` + low temperature; model overridable via `GEMINI_IMAGE_MODEL`. Switched from 2.5-flash-image because flash left grey "dunes" sweeps on busy urban source photos and didn't fully replace backgrounds. Pro is slower (reasoning model) + pricier (~$0.10–0.20/image), fine for one-off thumbnails. `CarImage` backs the slot with white (`bg-cloud`) so any near-white edge blends.
  - **↪ Fallback if quality/consistency regresses again: Imagen on Vertex AI.** Migrate `studio-shot` to **Imagen (Vertex AI)**, where Gemini's own advice actually applies — **subject masking / bounding-box** (force the car to occupy an exact % of the canvas, centre-anchored) and **ControlNet / depth maps** (pass a template depth map so every car matches the same silhouette + floor angle). True pixel-consistent framing, at the cost of a heavier integration (Vertex auth/project, different API). Only worth it if Nano Banana Pro proves inconsistent across many cars.

## Listing onboarding (add a car)
- **Photo-first listing with AI prefill.** Reorder the "List a car" flow so the **photo upload is step 1**. Then run a vision pass (Gemini, like studio-shot/verify-licence) on the uploaded image to **prefill make, brand, model, exterior colour, interior colour** (and optionally category) — the partner just reviews/edits. Big friction reducer.
  - _Notes / caveats:_ extraction of exact **model/trim** is less reliable than reading text (a 911 Carrera vs Turbo looks similar) — always keep fields editable and treat AI values as suggestions. **Interior colour** can't be read from an exterior-only shot — either ask for a second (interior) photo or leave interior for manual entry. Reuse/extend the studio-shot call so we generate the thumbnail AND extract specs in one go. Could also cross-check the AI make/model against the curated `carSpecs.js` to auto-fill hp/seats/etc.

## Fleet data import / export
- **Import via Excel / CSV.** An **Import** button in the fleet overview to bulk-add cars from a spreadsheet, with a **help panel + downloadable sample templates** (.csv / .xlsx) showing the required columns (make, model, year, category, city, price_per_day, colours, power, seats, gearbox, fuel, mileage, etc.). Parse client-side (e.g. SheetJS), validate rows, show a preview + per-row errors before committing, then batch-insert.
- **Export current data.** An **Export** button to download the partner's current fleet (and optionally bookings) as CSV/XLSX.

## Branding
- **Logo mark next to the wordmark.** The placeholder "AL" badge (`Icon.Logo` in an ink rounded square) was removed from the nav, partner login, dashboard sidebar and mobile licence page — for now it's just the "AIRLUXO" wordmark. Revisit with a real brand mark / monogram and place it before the wordmark (and use it as the favicon + embed "Powered by"). `Icon.Logo` still exists in `Icons.jsx` if we want to reuse the shape.

## Design polish
**Setup — ✅ DONE 2 Jun 2026 (skill installed + configured; the polish pass itself is still pending, deliberately held until after manual testing).**
- The **`impeccable`** design skill is installed and **fully operational** (verified: `node .claude/skills/impeccable/scripts/context.mjs` runs and loads both context files).
- **`PRODUCT.md`** + **`DESIGN.md`** exist at the repo root and are committed (commit `c6b3c35`). These are the design-context files every impeccable command reads first: PRODUCT.md = strategy (register `brand`, users, personality "quiet luxury · cinematic · concierge", anti-references, principles, WCAG 2.1 AA); DESIGN.md = the visual system captured from `src/index.css` tokens. **Keep both current** when the design or brand shifts.
- **Permissions:** three allow-rules in `.claude/settings.local.json` (gitignored, machine-local) let the skill's scripts + CLI run: `Bash(node .claude/skills/impeccable/scripts/*)`, `Bash(node .agents/skills/impeccable/scripts/*)`, `Bash(npx impeccable *)`. **These do not travel with the repo** — on a new machine/clone, re-add them (and re-install the skills via `npx`) before the skill will run.
- **Still pending (optional):** live-mode config (`.impeccable/live/config.json`) for `/impeccable live` in-browser variant editing — it self-configures on first run, best done during the actual polish pass with the dev server up.

- **UI/UX polish pass — the actual work, AFTER testing.** Once the current build is manually tested, run a design-quality pass over the front-end. Start: `/impeccable audit src/components/CarDetail.jsx` (a11y/contrast/responsive on the new protection UI) → `/impeccable critique` the marketplace home (scored UX review) → `/impeccable polish <target>` (pre-ship pass). Priority surfaces: the **damage-protection add-on toggle + price breakdown** in `CarDetail`, the **booking-flow micro-interactions** (date picker, step transitions, payment step), and the **partner dashboard forms** for consistency/tactile feel.
- **Installed design skills — which to reach for (all project-local in `.claude/skills/` + `.agents/skills/`, gitignored, scoped to AIRLUXO; invoke explicitly, none auto-activate; they run with full agent permissions so review before use).**
  - **Lead tool: `impeccable`** (full design system — sub-commands `audit · critique · polish · craft · shape · layout · typeset · colorize · animate · delight · clarify · optimize · harden · …`). Audit-first and **identity-preserving** (reads existing tokens/components, "if committed brand colors exist, identity-preservation wins"), ships real production code, iterates via browser screenshots. The right driver for refining a built app. **Prereq: `/impeccable init` was run → generates `PRODUCT.md` (design-context file it reads each run); keep it current.** For the polish pass: `impeccable audit` first, then `impeccable polish <target>`.
  - **Complements** (pull in for specifics): **`emil-design-eng`** (component states, animation/easing/timing, invisible details; most surgical), **`high-end-visual-design`** (agency-grade fonts/spacing/shadows), **`redesign-existing-projects`** (generic-AI-pattern audit).
  - **Aesthetic-imposing — greenfield only, would fight AIRLUXO's existing luxury language if applied wholesale:** `gpt-taste` (GSAP scroll-pinning + randomized bento), `minimalist-ui`, `industrial-brutalist-ui`, `stitch-design-taste`, `design-taste-frontend` (+ `design-taste-frontend-v1`). Good for starting a brand-new page from scratch, risky as a blanket pass.
  - **Image-generation only (no code — mockups/marketing):** `imagegen-frontend-web`, `imagegen-frontend-mobile`, `image-to-code`, `brandkit`. Produce design reference images, not React.
  - **Behavior-altering — use with caution:** `full-output-enforcement` (overrides output-truncation behavior, bans placeholders). Don't enable by default.

## Known gaps to revisit
- **Exact partner financials — persist commission + app-fee per booking.** The founder Partners detail sheet computes "our earnings / partner net / commission" from the partner's *current* plan rate (`est_*`), because bookings don't store the host commission or Stripe application fee. Fix: write `host_commission` + `application_fee` onto the booking at PaymentIntent creation (`stripe-create-payment` already computes both) + `buildPayload`, then the detail RPC can sum exact figures instead of estimating. Also handles plan changes over time and platform-vs-partner discount funding.
- **Availability date/time picker popup (booking flow) — DONE.** Calendar popup greys out booked/blocked dates, guest picks pickup + return dates and times; server-side availability check on submit prevents double-booking. Time picker now also respects the partner's opening hours (see logistics item above).
- **Marketplace browsing: date picker + advanced filters — DONE.** Homepage hero "When" pill is now a real date popover with three modes: **Any**, **Exact** (range calendar — shows cars free for the whole stay) and **Flexible** (This weekend / Next 7 / 30 / 90 days — shows cars with availability *somewhere* in the window). Availability comes from the `availability` edge function's new bulk `{ all: true }` mode (busy ranges per listing). The collection has an **advanced filters** panel: pick-up location, brand, colour, transmission, fuel, seats, min-power slider, max-price slider, cross-border-OK and delivery-available toggles, with an active-count badge + "Clear all". _Remaining:_ exact mode is a single-month calendar (no dual-month view); the per-car booking picker and the homepage browse picker are separate components (could be unified later).
- **Per-km delivery pricing** (v2 of the flat delivery fee) — needs a maps/geocoding API.
  - **↪ Different return location.** Once delivery pricing is distance-based (per-km), let the guest choose a **separate return address** from the pick-up/delivery address, priced on each leg's distance. Today delivery is a single address (collected at checkout); the return-elsewhere option waits on the km-distance calculation.
- ✅ **Overview KPIs are now real** — "Net earnings · <month>", the +/-% vs previous month, "Fleet utilisation" (booked car-days ÷ cars × days this month) and the 6-month payouts chart are all computed from real bookings (`computeMetrics`); the Earnings view chart is real too. Sample badges/notes removed.
- Guest accounts (currently guests book without an account).
- **Mobile: input focus zooms the page (booking flow + modals).** On iOS Safari, focusing an input with font-size < 16px triggers auto-zoom, which blows up the modal/popup. Fix: force **≥16px inputs on mobile** (e.g. an `@media (max-width:640px){ input,select,textarea{ font-size:16px } }` rule in `index.css`) rather than `maximum-scale=1` on the viewport (which hurts accessibility). Affects the booking modal, auth modal, and dashboard forms (`text-sm` inputs).
- Booking-notify abuse hardening (rate-limit / signed payload) and a proper verified email sender domain.
